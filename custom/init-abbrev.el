;;; package -- initialize abbrev config
;;; Commentary:

;;; Code:
(setq abbrev-file-name
      "~/.emacs.d/abbrev_defs")

(setq save-abbrevs t)
;(setq-default abbrev-mode t)

(provide 'init-abbrev)
;;; init-abbrev.el ends here
